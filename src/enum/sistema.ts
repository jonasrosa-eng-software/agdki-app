export enum UserStatus {
  onLine = 'success',
  offLine = 'danger',
  absent = 'warning',
}

export enum StatusExecuted {
  refresh = 'refresh',
  force_refresh = 'force_refresh',
  continue = 'continue',
}

export const statusExecuted = StatusExecuted
