import { MainClient } from "@/components/main/main";
import { HomeLayout } from "@/layouts/home-layout";

const HomeClient = () => {
  return (
    <HomeLayout>
      <MainClient />
    </HomeLayout>
  );
};

export { HomeClient };
